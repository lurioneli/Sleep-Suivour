{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid",
        ".validate": "newData.hasChildren()"
      }
    },
    "leaderboard": {
      "daily": {
        "$date": {
          ".read": "auth != null",
          "$uid": {
            ".write": "$uid === auth.uid",
            ".validate": "newData.hasChildren(['username', 'heartPoints', 'totalXP']) && newData.child('username').isString() && newData.child('username').val().length <= 20 && newData.child('heartPoints').isNumber() && newData.child('heartPoints').val() >= 0 && newData.child('heartPoints').val() <= 1000 && newData.child('totalXP').isNumber() && newData.child('totalXP').val() >= 0"
          },
          ".indexOn": ["heartPoints", "totalXP"]
        }
      },
      "alltime": {
        ".read": "auth != null",
        "$uid": {
          ".write": "$uid === auth.uid",
          ".validate": "newData.hasChildren(['username', 'heartPoints', 'totalXP']) && newData.child('username').isString() && newData.child('username').val().length <= 20 && newData.child('heartPoints').isNumber() && newData.child('heartPoints').val() >= 0 && newData.child('heartPoints').val() <= 10000 && newData.child('totalXP').isNumber() && newData.child('totalXP').val() >= 0"
        },
        ".indexOn": ["heartPoints", "totalXP"]
      }
    },
    "usernames": {
      "$username": {
        ".read": "auth != null",
        ".write": "auth != null && (!data.exists() || data.child('uid').val() === auth.uid)",
        ".validate": "newData.hasChild('uid') && newData.child('uid').val() === auth.uid && $username.length >= 3 && $username.length <= 20 && $username.matches(/^[a-zA-Z0-9_]+$/)"
      }
    },
    "forum": {
      "posts": {
        ".read": "auth != null",
        "$postId": {
          ".write": "auth != null && (!data.exists() || (data.exists() && newData.exists()) || (data.exists() && !newData.exists() && data.child('authorUid').val() === auth.uid))",
          ".validate": "(!data.exists() && newData.hasChildren(['id', 'content', 'authorUid', 'authorUsername', 'timestamp', 'likeCount']) && newData.child('content').isString() && newData.child('content').val().length >= 1 && newData.child('content').val().length <= 280 && !newData.child('content').val().matches(/https?:\\/\\/|www\\.|\\.(com|org|net|io|co|gg|me|tv|xyz|app|dev)/i) && newData.child('authorUid').val() === auth.uid && newData.child('authorUsername').isString() && newData.child('authorUsername').val().length <= 20 && newData.child('timestamp').isNumber() && newData.child('likeCount').isNumber() && newData.child('likeCount').val() >= 0 && (root.child('forum/rateLimit/' + auth.uid + '/lastPostTime').val() == null || newData.child('timestamp').val() - root.child('forum/rateLimit/' + auth.uid + '/lastPostTime').val() >= 60000)) || (data.exists() && newData.exists() && data.child('id').val() === newData.child('id').val() && data.child('content').val() === newData.child('content').val() && data.child('authorUid').val() === newData.child('authorUid').val() && data.child('authorUsername').val() === newData.child('authorUsername').val() && data.child('timestamp').val() === newData.child('timestamp').val() && newData.child('likeCount').isNumber() && newData.child('likeCount').val() >= 0 && newData.child('likeCount').val() <= 10000)"
        },
        ".indexOn": ["timestamp", "authorUid"]
      },
      "likes": {
        "$postId": {
          ".read": "auth != null",
          "$uid": {
            ".write": "$uid === auth.uid",
            ".validate": "(newData.val() === true || !newData.exists()) && (root.child('forum/rateLimit/' + auth.uid + '/lastLikeTime').val() == null || now - root.child('forum/rateLimit/' + auth.uid + '/lastLikeTime').val() >= 3000)"
          }
        }
      },
      "userPosts": {
        "$uid": {
          ".read": "auth != null",
          ".write": "$uid === auth.uid"
        }
      },
      "rateLimit": {
        "$uid": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid",
          ".validate": "newData.hasChildren()",
          "lastPostTime": {
            ".validate": "newData.isNumber() && newData.val() <= now"
          },
          "lastLikeTime": {
            ".validate": "newData.isNumber() && newData.val() <= now"
          }
        }
      },
      "reports": {
        "$postId": {
          ".read": false,
          "$uid": {
            ".write": "$uid === auth.uid && !data.exists()",
            ".validate": "newData.hasChildren(['reportedAt', 'reporterUid', 'postAuthorUid']) && newData.child('reporterUid').val() === auth.uid && newData.child('reportedAt').isNumber() && newData.child('reportedAt').val() <= now"
          }
        }
      }
    }
  }
}
